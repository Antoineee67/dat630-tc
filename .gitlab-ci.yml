default:
  image: docker:25.0.5
  services:
    - docker:25.0.5-dind
  before_script:
    - docker info



build:
  stage: build
  tags:
    - docker
  script:
    - docker build -t treecode .


test:
  stage: test
  tags:
    - docker
  script:
    - docker run treecode ./tools/verifyOutput.sh



